<nav class="nav">
  <% if current_user %>
    <p> Welcome:  <%=current_user.name %></p>
    <ul>
      <li><%= link_to "Recipes",  user_recipes_path(user_id: current_user.id), class: "links" %></li>
      <li><%= link_to "Foods", user_foods_path(user_id: current_user.id), class: "links" %></li>
      <li><%= link_to "Public Recipes", publicrecipes_path(), class: "links" %></li>
      <%= link_to "Log Out", destroy_user_session_path,data: { turbo_method: :delete }, class: "links" %>
    </ul>
  <% else %>
    <ul>
      <li><%= link_to "Sign Up", new_user_registration_path, class: "links" %></li>
      <li><%= link_to "Log In", new_user_session_path, class: "links" %></li>
    </ul>
  <% end %>
</nav>
